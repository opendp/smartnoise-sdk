SELECT SUM(Age + Income) FROM PUMS.PUMS;
SELECT SUM(SUM(age)) FROM PUMS.PUMS;
SELECT TOP 5 educ, AVG(income) FROM PUMS.PUMS GROUP BY income ORDER BY income DESC;
SELECT COUNT(*) FROM (SELECT * FROM PUMS.PUMS);
SELECT a.pid, b.income FROM PUMS.PUMS AS a JOIN PUMS.PUMS AS b ON b.pid = a.pid;
SELECT married, educ, income, AVG(age) FROM PUMS.PUMS;
SELECT 3 AS three, p.married, educ, AVG(income) FROM PUMS.PUMS AS p GROUP BY educ;
SELECT CASE WHEN age > 90 THEN 1 ELSE 0 END AS age FROM PUMS.PUMS;
SELECT SUM(CASE WHEN age > 90 THEN 1 ELSE 0 END) AS age FROM PUMS.PUMS;
SELECT SUM(age + 1) * 1 AS age FROM PUMS.PUMS;
