name: tutorial

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      dataset_name: str
      column_name: str
      budget: double
    command: "python calculate.py {dataset_name} {column_name} {budget}"
